import { GuildConfig as PrismaGuildConfig } from '@prisma/client';

describe('ConfigTranslate', () => {
  describe('yeah', () => {
    it('true', async () => {
      expect(true).toBe(true);
    });
  });
});
